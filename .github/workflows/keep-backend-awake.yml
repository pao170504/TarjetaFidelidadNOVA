name: Mantener despierto el backend

on:
  schedule:
    - cron: "*/14 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hacer ping al backend en Render
        run: curl -sf https://tarjetafidelidad-backend.onrender.com/clientes -o /dev/null
